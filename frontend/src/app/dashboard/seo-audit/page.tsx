import ToolJobList from "@/components/ToolJobList";

export default function SeoAuditPage() {
  return (
    <ToolJobList 
      toolName="Technical SEO Audit" 
      toolPath="seo-audit"
      description="Identify broken links, missing meta tags, and accessibility issues that are holding back your search engine rankings."
    />
  );
}
