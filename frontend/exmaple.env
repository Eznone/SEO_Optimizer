# Server-side Backend URL (used by Next.js rewrites)
BACKEND_URL="http://localhost:8000"

# Client-side Backend URL (used for absolute redirects like OAuth)
NEXT_PUBLIC_BACKEND_URL="http://localhost:8000"
